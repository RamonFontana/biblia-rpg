# Quickstart

## Local Testing
1. Run `npm run dev`
2. Open the active session page on a mobile device or using Chrome DevTools device toolbar (e.g., iPhone SE size).
3. Verify that the NPC cards, Player cards, and Combat Tracker elements do not overflow their containers horizontally.
